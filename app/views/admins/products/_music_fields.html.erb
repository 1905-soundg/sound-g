<div class="nested-fields form-inline">
	<div class="form-group">
		<p>曲名</p>
		<%= f.text_field :title %>
	</div>

	<div class="form-group">
		<p>トラックナンバー</p>
		<%= f.text_field :track_number %>
	</div>

	<div class="form-group">
		<%= f.collection_select :artist_id, Artist.all, :id, :artist %>
	</div>

</div>