<div class='row'>
<div class="col-md-8 col-md-offset-2">
    <div class="row">
    <div class="col-md-8 col-md-offset-2">
    	<h2>ご注文の確認</h2>

		<p class="" style="margin: 30px 0 30px 0;">小計		¥<%= get_price(@carts) %></p>
		<p class="" style="margin: 30px 0 30px 0;">送料		¥<%= @fee %></p>
		<p class="" style="margin: 30px 0 30px 0;">請求額	¥<%= get_totalprice(@carts) %>(税込)</p>

        <h2>お届け先住所の選択</h2>
        <h1></h1>
		<%= form_for @order, url:user_orders_path(user_id: @user) do |f| %>
		        <ul class="list-unstyled">
		        	<li>
		        	<h4>ご自宅</h4>
		        	<label style="font-weight: normal;">
		        		<%= f.radio_button :address, @user.address  %>
		        			<%= @user.address %>
	                        <%= @user.postalcode %>
							<%= @user.last_name + @user.first_name %>
		        	</label>
		        	</li>
		        	<h4>ご自宅以外のお届け先</h4>
		        	<% @addresses.each do |address| %>
		            <li>
	                <label style="font-weight: normal;">
	                <%= f.radio_button :address, "#{address.id} #{address.address}" %>
	                〒<%= address.postalcode %>
	                <%= address.address %>
	                <%= address.name %>
	                </label>
		            </li>
		             <% end %>
		        </ul>

                <%= f.hidden_field :subtotal_price, :value => get_price(@carts) %>
				<%= f.hidden_field :fee, :value => @fee %>
				<%= f.hidden_field :order_status, :value => "販売中" %>

				<h2>お支払い方法</h2>
				<div class="radio">
  					<label><input type="radio" name="radio" id="radio1" value="radio1"> 銀行振込</label>
				</div>
				<div class="radio">
				  	<label><input type="radio" name="radio" id="radio2" value="radio2"> クレジットカード</label>
				</div>
				<div class="radio disabled">
				 	 <label><input type="radio" name="radio" id="radio3" value="radio3"> 代金引換</label>
				</div>

			<%= f.submit "購入" ,class: "btn-sm btn-primary btn-block" %>
        <% end %>
		</div>
		</div>
	</div>
    </div>
</div>
</div>